<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_form_view" model="ir.ui.view">
        <field name="name">product.template.form.inherit</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='pricing']" position="after">
                <label for="discount_percentage"/>
                <div name="discount_percentage" class="o_row">
                    %
                    <field name="discount_percentage" nolabel="1" class="oe_inline" />
                </div>
                <field name="discounted_price" /> 
            </xpath>
        </field>
    </record>

    <!-- <template id="product_price" inherit_id="website_sale.product">
        <xpath expr="//span[@itemprop='price']" position="replace">
            <t t-if="product.discount_percentage > 0">
                <span class="oe_currency_value" t-field="product.discounted_price"/>
                <span class="oe_currency_value" t-field="product.list_price" style="text-decoration: line-through;"/>
            </t>
            <t t-else="">
                <span class="oe_currency_value" t-field="product.list_price"/>
            </t>
        </xpath>
    </template> -->

</odoo>